/**
 * @format
 */

import {AppRegistry} from 'react-native';
import SalesApp from './src/sales/App';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => SalesApp);

